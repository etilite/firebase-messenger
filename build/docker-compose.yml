version: '3.8'

services:
  firebase-messenger:
    image: etilite/firebase-messenger:latest
    build:
      context: ./../
      dockerfile: ./build/Dockerfile
    container_name: firebase-messenger
    ports:
      - "8080:8080"
    environment:
      - HTTP_HOST_PORT=:8080